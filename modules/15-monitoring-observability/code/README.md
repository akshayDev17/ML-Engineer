# M15 · code/ — Pass 2 demos

Working code for this module's demos lands here in **Pass 2** — nothing here yet by design.

Each file's spec is captured in the `💻 CODE (Pass 2)` blockquotes in [`../README.md`](../README.md). Base reference: [`tech/alibi-detect.md`](../../tech/alibi-detect.md).

**Planned files:**

| File | Demonstrates |
|---|---|
| `drift_alerting.py` | The drift detector (from M16's `drift_detector.py`) wired into the monitoring loop: p-values → Prometheus gauges → alert — the "drift p-value becomes a page" arc |
